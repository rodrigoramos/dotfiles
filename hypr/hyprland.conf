# See https://wiki.hyprland.org/Configuring/Monitors/
#monitor=name,resolution,position,scale

## Configuração Casa
monitor=eDP-1,preferred,0x0,1.1
monitor=HDMI-A-1, 3840x2160,1743x0,1.3
monitor=DP-7,preffered,4700x0,auto

## Configuração Campinas
#monitor=HDMI-A-1,preferred,0x0,1
#monitor=eDP-1,preferred,0x1050,1.1

# See https://wiki.hyprland.org/Configuring/Keywords/ for more

# Execute your favorite apps at launch
# exec-once = waybar & hyprpaper & firefox
$ags=~/.local/state/nix/profiles/profile/bin/ags
exec-once = $ags
#exec-once = foot --server
#exec-once = wlsunset -l -23.5489 -L -46.6388 
#exec-once = swayidle -w timeout 300 'hyprctl dispatch dpms off' resume 'hyprctl dispatch dpms on'

# Source a file (multi-file configs)
# source = ~/.config/hypr/myColors.conf

# For all categories, see https://wiki.hyprland.org/Configuring/Variables/
input {
    kb_layout = br
    kb_variant = thinkpad
    kb_model =
    kb_options = caps:escape
    kb_rules =

    # 0 - Cursor movement will not change focus.
    # 1 - Cursor movement will always change focus to the window under the cursor.
    # 2 - Cursor focus will be detached from keyboard focus. Clicking on a window will move keyboard focus to that window.
    # 3 - Cursor focus will be completely separate from keyboard focus. Clicking on a window will not change keyboard focus.
    follow_mouse = 2
    
    touchpad {
        natural_scroll = yes
    }
}


general {
    # See https://wiki.hyprland.org/Configuring/Variables/ for more

    gaps_in = 5
    gaps_out = 10
    border_size = 1.6
    col.active_border = rgba(33ccffee) rgba(00ff99ee) 45deg
    col.inactive_border = rgba(595959aa)

    layout = dwindle
}

misc {
  vrr = 1
  suppress_portal_warnings = true
}

decoration {
    # See https://wiki.hyprland.org/Configuring/Variables/ for more

    rounding = 5 
    blur = yes
    blur_size = 4
    blur_passes = 2
    blur_new_optimizations = on

    drop_shadow = yes
    shadow_range = 4
    shadow_render_power = 3
    col.shadow = rgba(1a1a1aee)
}

animations {
    enabled = yes

    # Some default animations, see https://wiki.hyprland.org/Configuring/Animations/ for more

    bezier = myBezier, 0.05, 0.9, 0.1, 1.05

    animation = windows, 1, 7, myBezier
    animation = windowsOut, 1, 7, default, popin 80%
    animation = border, 1, 10, default
    animation = fade, 1, 7, default
    animation = workspaces, 1, 6, default
}

dwindle {
    # See https://wiki.hyprland.org/Configuring/Dwindle-Layout/ for more
    pseudotile = yes # master switch for pseudotiling. Enabling is bound to mainMod + P in the keybinds section below
    preserve_split = yes # you probably want this
    force_split = 2 # Split to right or bottom
}

master {
    # See https://wiki.hyprland.org/Configuring/Master-Layout/ for more
    new_is_master = false
    no_gaps_when_only = true
}

gestures {
    # See https://wiki.hyprland.org/Configuring/Variables/ for more
    workspace_swipe = off
}

# Example per-device config
# See https://wiki.hyprland.org/Configuring/Keywords/#executing for more
device:epic mouse V1 {
    sensitivity = -0.5
}

device:keychron-k6 {
    kb_layout = us_custom
    kb_options = caps:escape
    kb_model =
    kb_variant = 
}

# Example windowrule v1
# windowrule = float, ^(kitty)$
# Example windowrule v2
# windowrulev2 = float,class:^(kitty)$,title:^(kitty)$
# See https://wiki.hyprland.org/Configuring/Window-Rules/ for more


# See https://wiki.hyprland.org/Configuring/Keywords/ for more
$mainMod = SUPER

# Example binds, see https://wiki.hyprland.org/Configuring/Binds/ for more
bind = $mainMod, Return, exec, gnome-terminal
bind = $mainMod SHIFT, Q, killactive, 
bind = $mainMod SHIFT, Space, togglefloating, 
bind = $mainMod, d, exec, wofi --show drun
bind = $mainMod, f, fullscreen, 0

bind = $mainMod, j, workspace, m+1
bind = $mainMod, k, workspace, m-1

bindl = ,XF86MonBrightnessUp, exec, lgsight -A 5
bindl = ,XF86MonBrightnessDown, exec, light -U 5
bindl = ,XF86AudioPlay, exec, playerctl play-pause
bindl = ,XF86AudioPrev, exec, playerctl previous
bind = ,XF86PowerOff, exec, shutdown now

## Custom Shortcuts

# Mute Mic
bindl = $mainMod,m, exec, ~/git/dotfiles/bin/mute-default-mic
bindl = ,XF86AudioMicMute, exec, ~/git/dotfiles/bin/mute-default-mic

# Restart AGS
bind = $mainMod SHIFT, Z, exec, $ags -q && $ags

# notification center
bind = $mainMod, n, exec, $ags -t notification-center

# Next Calendar Events
bind = $mainMod, o, exec, $ags -t calendarEvents

# Calendar
bind = $mainMod SHIFT, o, exec, $ags -t dashboard

# GitHub PRs
bind = $mainMod, g, exec, xdg-open https://github.com/pulls/review-requested

# Screenshot
bind = $mainMod,p, exec, /usr/share/sway/scripts/grimshot save window - | wl-copy && notify-send "Screenshot clipboard"
bind = $mainMod SHIFT,p, exec, /usr/share/sway/scripts/grimshot save window - | swappy -f -

# Notebook lid handle
bindl=,switch:on:Lid Switch,exec, sleep 1 && hyprctl dispatch dpms off
bindl=,switch:off:Lid Switch,exec, sleep 1 && hyprctl dispatch dpms on


# Move focus with mainMod + arrow keys
bind = $mainMod, left, movefocus, l
bind = $mainMod, right, movefocus, r
bind = $mainMod, up, movefocus, u
bind = $mainMod, down, movefocus, d

# Move windows inside the workspace
bind = $mainMod SHIFT, up, movewindow, u
bind = $mainMod SHIFT, down, movewindow, d
bind = $mainMod SHIFT, right, movewindow, r
bind = $mainMod SHIFT, left, movewindow, l

bind = $mainMod SHIFT, j, swapnext,
bind = $mainMod SHIFT, k, swapnext, prev

# Switch workspaces with mainMod + [0-9]
bind = $mainMod, 1, workspace, 1
bind = $mainMod, 2, workspace, 2
bind = $mainMod, 3, workspace, 3
bind = $mainMod, 4, workspace, 4
bind = $mainMod, 5, workspace, 5
bind = $mainMod, 6, workspace, 6
bind = $mainMod, 7, workspace, 7
bind = $mainMod, 8, workspace, 8
bind = $mainMod, 9, workspace, 9
bind = $mainMod, 0, workspace, 10

# Move active window to a workspace with mainMod + SHIFT + [0-9]
bind = $mainMod SHIFT, 1, movetoworkspace, 1
bind = $mainMod SHIFT, 2, movetoworkspace, 2
bind = $mainMod SHIFT, 3, movetoworkspace, 3
bind = $mainMod SHIFT, 4, movetoworkspace, 4
bind = $mainMod SHIFT, 5, movetoworkspace, 5
bind = $mainMod SHIFT, 6, movetoworkspace, 6
bind = $mainMod SHIFT, 7, movetoworkspace, 7
bind = $mainMod SHIFT, 8, movetoworkspace, 8
bind = $mainMod SHIFT, 9, movetoworkspace, 9
bind = $mainMod SHIFT, 0, movetoworkspace, 10

# Scroll through existing workspaces with mainMod + scroll
bind = $mainMod, mouse_down, workspace, e+1
bind = $mainMod, mouse_up, workspace, e-1

bind = $mainMod, Tab, workspace, previous
bind = $mainMod, b, workspace, previous

# Move/resize windows with mainMod + LMB/RMB and dragging
bindm = $mainMod, mouse:272, movewindow
bindm = $mainMod, mouse:273, resizewindow

#
# Move Workspace
#
$move_workspaces=Move Workspace to (n: eDP-1; l:DP-7; j:HDMI-A-1)
bind=$mainMod SHIFT,m,submap,$move_workspaces

submap=$move_workspaces

binde=,n,movecurrentworkspacetomonitor,eDP-1 
binde=,l,movecurrentworkspacetomonitor,DP-7
binde=,j,movecurrentworkspacetomonitor,HDMI-A-1

bind=,escape,submap,reset 
bind=,return,submap,reset 
submap=reset

#
# Resize
#
bind=$mainMod,r,submap,Resize

submap=Resize

binde=,right,resizeactive,10 0
binde=,left,resizeactive,-10 0
binde=,up,resizeactive,0 -10
binde=,down,resizeactive,0 10

bind=,escape,submap,reset 
bind=,return,submap,reset 
submap=reset

#
# Shutdown
#
$shutdown=(e)xit, (s)hutdown, (r)eboot, (l)ock
bind=$mainMod SHIFT,e,submap,$shutdown

submap=$shutdown

binde=,e,exit,
binde=,r,exec,reboot
binde=,s,exec,shutdown now
binde=,l,exec,swaylock

bind=,escape,submap,reset 
submap=reset


# 
# Aplication x Workspaces
#

windowrule=workspace 2,^(code)$
windowrule=workspace 3,^(firefox)$
windowrule=workspace 5,title:^(Microsoft Teams.*)$
windowrule=workspace 4,title:^(Slack)$
windowrule=workspace 6,Logseq
windowrule=workspace 6,obsidian
windowrule=workspace 9,title:^(Spotify.*)$

# Slack Circles
windowrule=workspace 5,title:^(Círculo.*)$
windowrule=maximize,title:^(Círculo.*)$

windowrule=float,^(Lxappearance)$
windowrule=float,^(KeePassXC)$
windowrule=float,^(gnome-calculator)$
windowrulev2=float,class:^(firefox)$,title:^(Library)$
windowrulev2=float,class:^(firefox)$,title:^(.*Sharing Indicator.*)$
windowrulev2=float,title:^(Save File)$
windowrulev2=float,title:^(.* is sharing your screen.)$
windowrulev2=float,title:^(wlay)$

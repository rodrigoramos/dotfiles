Acrescentar no dotfile

Arquivos de profile do terminal

bashrc para alterar o arquivo
[ -z "$TMUX"  ] && { tmux attach || exec tmux new-session && exit;}

bashrc não pode ter variável de tema

Acrescentar o cake completion?
